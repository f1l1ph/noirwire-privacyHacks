# Shared environment variables (examples)

# Solana
SOLANA_RPC_URL=https://api.devnet.solana.com
SOLANA_NETWORK=devnet

# Supabase (local development)
SUPABASE_URL=http://localhost:54321
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Redis
REDIS_URL=redis://localhost:6379

# API
API_PORT=8080
API_URL=http://localhost:8080
CORS_ORIGIN=http://localhost:3000

# Indexer
INDEXER_PORT=8081

# ============================================
# MagicBlock PER (Private Execution Runtime)
# ============================================
#
# PER Configuration Guide:
# 1. Obtain API key from: https://docs.magicblock.gg
# 2. Configure endpoint based on network:
#    - Mainnet: https://tee.magicblock.app
#    - Devnet: https://tee.magicblock.app (or dev endpoint from MagicBlock)
#    - Localnet: http://localhost:9000 (local testing)
# 3. Test connectivity: Run `yarn test:per` after configuration
#
# SECURITY NOTES:
# - Always use HTTPS endpoints in production
# - Never commit API keys to git (use .env, not .env.example)
# - Enable attestation verification in production (PER_VERIFY_ATTESTATION=true)
# - Keep API keys secure and rotate regularly
#

# MagicBlock TEE Endpoint URL
# Required for vault operations and privacy-preserving queries
MAGICBLOCK_TEE_ENDPOINT=https://tee.magicblock.app

# MagicBlock API Authentication Key
# Obtain from: https://docs.magicblock.gg/authentication
# Leave empty for public endpoints (if available)
MAGICBLOCK_API_KEY=

# PER Request Timeout (milliseconds)
# Recommended: 30000 (30 seconds)
# Adjust based on network conditions
PER_TIMEOUT=30000

# Enable TEE Attestation Verification
# Production: true (REQUIRED)
# Development/Localnet: false (optional)
PER_VERIFY_ATTESTATION=true

# PER Retry Configuration
# Maximum number of retry attempts for failed requests
PER_MAX_RETRIES=3

# Initial retry delay in milliseconds (exponential backoff)
PER_RETRY_DELAY=1000

# Legacy/Alternative PER Endpoint (if using custom implementation)
PER_EXECUTOR_URL=https://per.noirwire.com
